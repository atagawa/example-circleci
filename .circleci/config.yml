# $ circleci config validate -> ファイルのバリデーションチェックを実行
# $ circleci local execute -> localで本ファイルを実行
version: 2
jobs:
  build:
    docker:
      - image: cimg/node:lts
    steps:
      - checkout
      - run: yarn install
      - run: yarn jest
